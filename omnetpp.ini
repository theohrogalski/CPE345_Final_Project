[General]
#scheduler-class = "inet::cRealTimeScheduler" #so that speed appears realistic
sim-time-limit = 3000s
user-interface = Qtenv

#abstract-config = true (requires omnet 7)

[Config Update]
#abstract-config = true (requires omnet 7)
**.updateInterval = 0.1s # test with 0s too, and let getCurrentPosition update the display string from a test module
**.mobility.initFromDisplayString = true
# This is the "Master Switch" for visual movement
*.visualizer.mobilityVisualizer.displayPositions = true
*.visualizer.mobilityVisualizer.displayVelocities = true

# This ensures the canvas matches your coordinate system
*.visualizer.canvasVisualizer.sceneVisualizer.viewAngle = "xy"
**.displayStringTextFormat ="p: %p\nv: %v" #format string for the mobility module's display string text
*.updateInterval = 0.1s
*.faceForward = false
[PersistentMonitoring]
sim-time-limit = 3000s
cmdenv-autoflush = true
cmdenv-express-mode = false  # This shows every single event, not just summaries
network = PersistentMonitoringNetwork

**.networkConfiguratorModule = ""
# 2. Turn on the trails and arrows
*.visualizer.mobilityVisualizer.displayMovementTrails = true
*.visualizer.mobilityVisualizer.displayVelocities = true
*.visualizer.mobilityVisualizer.displayOrientations = true

# 3. Customize the trail lengths (so the screen doesn't get too messy)
*.visualizer.mobilityVisualizer.trailLength = 200
**.num_agents = 3
